#include <stdio.h>

/**
 * main - prints the digits in base 10
 * followed by a new line
 * Return: 0
 */

int main(void)
{
	int i;

	for (i == 0; i <= 9; i++)
		putchar(i);
	putchar("\n");

	return (0);

}
